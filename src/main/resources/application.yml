spring:
  servlet:
    multipart:
      enabled=true
      max-file-size=10MB
      max-request-size=10MB
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console #http://localhost:8080/h2-console


google:
  cloud:
    vision:
      api-key: AIzaSyBF4R-ZeKr9ExIS_Xm4yenLyVYepNwHjeM
